import { GoogleMaps } from "../GoogleMaps";
import { Header } from "../Header";

export const LoggedStartLayout = ( ) => {
    return (
        <div>
            <Header />
            <GoogleMaps/>
        </div>
    )
}